-- script that lists all records of the table second_table
-- Results set display both the score and the name and
-- should be ordered by the score
SELECT score, name
FROM second_table
ORDER BY score DESC;
